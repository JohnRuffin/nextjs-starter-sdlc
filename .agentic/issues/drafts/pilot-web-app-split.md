# [TASK] Pilot the split web-app flow

## Context

This pilot issue validates the split-topology agentic SDLC overlay for nextjs-starter-sdlc using a minimal, verifiable homepage change.

## Requirements

- use `topology:split`
- prove the planner-to-builder handoff in GitHub issue comments or linked PR context
- add a visible heading to the homepage
- stay within a single page file
- avoid routing, auth, API, or architecture changes

## Acceptance Criteria

- the homepage renders an `<h1>` with the text "Welcome to nextjs-starter-sdlc"
- the handoff evidence is recorded in the issue or PR
- Playwright can assert the heading is visible (`page.getByRole('heading', { level: 1 })`)
- `pnpm build` passes
- task-relevant verification is recorded in the PR

## Target Files

- `app/page.tsx` or `src/app/page.tsx`

## Notes

This is the split-topology seed issue generated by `agentic-sdlc init --profile web-app`.
