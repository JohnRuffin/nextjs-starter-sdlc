# [TASK] Add a welcome heading to the homepage

## Context

This pilot issue validates the combined-topology agentic SDLC overlay for nextjs-starter-sdlc using a minimal, verifiable homepage change.

## Requirements

- use `topology:combined`
- add a visible heading to the homepage
- stay within a single page file
- avoid routing, auth, API, or architecture changes

## Acceptance Criteria

- the homepage renders an `<h1>` with the text "Welcome to nextjs-starter-sdlc"
- Playwright can assert the heading is visible (`page.getByRole('heading', { level: 1 })`)
- `pnpm build` passes
- task-relevant verification is recorded in the PR

## Target Files

- `app/page.tsx`
- `src/app/page.tsx`

## Notes

This is the combined-topology seed issue generated by `agentic-sdlc init --profile web-app`.
