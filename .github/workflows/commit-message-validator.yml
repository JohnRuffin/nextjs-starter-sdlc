name: Commit Message Validator Example

on:
  pull_request:
    types: [opened, synchronize, edited]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate commit or PR title convention
        run: |
          echo "Example workflow only"
          echo "Implement repository-specific commit or PR title convention checks."
