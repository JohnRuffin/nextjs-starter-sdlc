name: PR Contract Validator Example

on:
  pull_request:
    types: [opened, edited, synchronize, ready_for_review]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate PR contract
        run: |
          echo "Example workflow only"
          echo "Run a portable PR contract validator such as templates/scripts/validate-pr.js."
